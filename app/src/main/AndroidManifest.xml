<?xml version="1.0" encoding="utf-8"?>
<!-- アプリのマニフェスト：権限、コンポーネント、ウィジェットを宣言 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- カレンダー操作に必要な権限（Googleモード時） -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <!-- 通知表示に必要な権限（Android 13+） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 正確なアラーム設定に必要な権限（Android 12+） -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- バッテリー最適化を無効化するための権限（通知遅延防止） -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 端末再起動時に通知を再スケジュールするための権限 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 祝日データ取得のためのインターネット通信 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".GolenderApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <!-- メインアクティビティ（ランチャー） -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 通知受信用ブロードキャストレシーバー（アプリ内完結） -->
        <receiver
            android:name=".notification.NotificationReceiver"
            android:exported="false" />

        <!-- 端末起動時/アプデ時に通知を再スケジュール -->
        <receiver
            android:name=".notification.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- 日次ウィジェット（今日の予定を表示） -->
        <receiver
            android:name=".widget.DayWidgetReceiver"
            android:label="1日の予定"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/day_widget_info" />
        </receiver>

        <!-- 週間ウィジェット（週間予定を表示） -->
        <receiver
            android:name=".widget.WeekWidgetReceiver"
            android:label="週間カレンダー"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/week_widget_info" />
        </receiver>

        <!-- 月間ウィジェット（月間カレンダーグリッド） -->
        <receiver
            android:name=".widget.MonthWidgetReceiver"
            android:label="月間カレンダー"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/month_widget_info" />
        </receiver>

    </application>

</manifest>